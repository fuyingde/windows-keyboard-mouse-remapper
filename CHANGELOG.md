# 更新日志

## 1.6

- 支持最多三个按键的组合映射；最后一个映射前按键按下时立即生效，任一原始按键松开后解除
- 映射前检查前缀冲突，例如 Ctrl 与 Ctrl+C 不能同时保存；映射后不限制重复
- 鼠标左/右键不能作为原始按键单独映射，但可以作为映射目标
- 滚轮只能位于原始组合键的最后一位
- 按键冲突和不得映射的提示显示在左侧提示区
- 优化鼠标侧键映射，修复侧键导致界面空白
- WebView2 用户数据改到 `%LocalAppData%\键鼠映射\WebView2`
- 保存映射时清理 INI 中已删除的残留段落

## 1.5

- 增加开机自启（当前用户注册表）
- 增加隐藏托盘图标
- 增加关于窗口

## 1.4

- 限制只能运行一个实例，防止多开导致映射混乱

## 1.3

- 映射配置改存到 `%AppData%\键鼠映射\键鼠映射.ini`

## 1.2

- 优化界面
- 限制同一个原始按键被多次映射

## 1.1

- 点击录制框自动获取按键编码
- 禁止映射鼠标左键和右键

## 1.0

- 首次发布：键盘和鼠标按键重映射

